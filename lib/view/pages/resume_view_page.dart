import 'package:flutter/material.dart';

class ViewResumePAge extends StatelessWidget {
  const ViewResumePAge({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}